# AGENTS.md

## 项目概述

- 当前仓库 `gox` 是一个 Go 基础工具库仓库，包含 `xslice`、`xmap`、`xsync`、`xget`、`xpack`、`xptr`、`xconv` 等通用能力。
- 这里的代码以“可复用、低心智负担、少依赖”为第一原则，不应混入业务语义。

## 编码风格

- 函数实现要有清晰的结构感。常见步骤如参数校验、核心逻辑、结果组装之间需要使用空行分隔，提升可读性。
- 优先选择直接、稳定、容易维护的实现，不为了抽象而抽象。
- 保持现有包风格一致：命名简洁、职责单一、接口语义明确。
- 对导出函数尤其注意兼容性，避免无必要地改变已有行为。
- 处理集合、map、指针、并发、泛型等能力时，优先延续仓库现有模式，而不是引入新的风格分支。

## 变更原则

- 新能力优先放到职责最匹配的包内，不要为了局部方便打破包边界。
- 如果需求会改变旧函数语义，优先考虑新增函数、增加可选能力，或保持旧行为不变。
- 工具函数需要明确处理边界情况，尤其包括：
- `nil`
- 空切片
- 空 map
- 零值
- 并发场景下的错误和 panic 传播

## 验证要求

- 修改公共函数时，应同步检查对应测试文件是否需要补充或更新。
- 即使本次未执行远程测试，也应完成自检，确认至少不存在明显的命名错误、语义偏差、包职责错位或可见的兼容性风险。
